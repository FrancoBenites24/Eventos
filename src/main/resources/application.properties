# Database Configuration
spring.datasource.url=jdbc:postgresql://eventosbd.cdekmioe2oqu.us-east-2.rds.amazonaws.com:5432/BDeventia
spring.datasource.username=postgres
spring.datasource.password=Leyenda29
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.batch_size=20
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=America/Lima

# Logging Configuration
logging.level.eventos.piura=INFO
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

logging.level.root=ERROR
logging.level.org.springframework=ERROR

# Server Configuration
server.port=8081

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin123

# File Upload Configuration
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Cache Configuration
spring.cache.type=redis
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.password=

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=francobenites24@gmail.com
spring.mail.password=mjqqowxtftrvdfvj
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# Mail application settings
app.mail.from-name=Eventos Piura
app.mail.verification-base-url=http://localhost:8081
app.mail.verification-expiration-hours=24
app.mail.reset-base-url=http://localhost:8081
app.mail.reset-expiration-minutes=60

# Application Specific Properties
app.name=Eventos Piura
app.version=1.0.0
app.timezone=America/Lima
app.currency=PEN
app.max-events-per-organizer=50
app.max-tickets-per-event=10000
app.reservation-timeout-minutes=15
app.coupon-usage-limit=1
app.points-per-attendance=10
app.points-per-review=5

spring.mvc.hiddenmethod.filter.enabled=true
